{
  "name": "clauge-auth-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.6",
    "@cloudflare/vitest-pool-workers": "^0.16.6",
    "wrangler": "^3"
  }
}
